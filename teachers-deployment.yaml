apiVersion: apps/v1
kind: Deployment
metadata:
  name: thinkflow-teachers
  namespace: thinkflow
spec:
  replicas: 2
  selector:
    matchLabels:
      app: thinkflow-teachers
  template:
    metadata:
      labels:
        app: thinkflow-teachers
    spec:
      containers:
        - name: thinkflow-teachers
          image: omkarbhute/thinkflow-teachers:latest
          ports:
            - containerPort: 5000
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "400m"
              memory: "512Mi"
